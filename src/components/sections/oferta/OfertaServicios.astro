---
// ============================================
// SABERO Consulting - Servicios y Soluciones
// Bloque 2: Servicios + Soluciones (2 cards)
// ============================================
import { ofertaContent } from '../../../data/home/oferta';

interface Props {
  locale: string;
}

const { locale } = Astro.props;
const content = ofertaContent[locale] || ofertaContent.es;
const { capacidades } = content;

// Only first 2 items: Servicios, Soluciones
const items = capacidades.items.slice(0, 2);
---

<section class="oferta-servicios">
  <div class="container">
    <div class="servicios-header">
      <span class="servicios-number">01</span>
      <h3 class="servicios-title">Servicios y soluciones</h3>
      <p class="servicios-subtitle">Qué hacemos</p>
    </div>

    <div class="servicios-grid">
      {items.map((item, i) => (
        <article class="serv-card" id={item.id}>
          <div class="serv-card-num-wrap">
            <span class="serv-card-num">0{i + 1}</span>
          </div>
          <h4 class="serv-card-title">{item.title}</h4>
          <p class="serv-card-summary">{item.summary}</p>
          <div class="serv-card-resolves">
            <span class="serv-card-resolves-label">Qué resuelve</span>
            <p class="serv-card-resolves-text">{item.resolves}</p>
          </div>
          <ul class="serv-card-list">
            {item.items.map((sub) => (
              <li class="serv-card-item">{sub}</li>
            ))}
          </ul>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .oferta-servicios {
    padding: 120px 0 140px;
    background: #FFFFFF;
    position: relative;
  }

  .oferta-servicios::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: calc(100% - 48px);
    max-width: 1200px;
    height: 1px;
    background: linear-gradient(90deg, transparent, #D0D8E4, transparent);
  }

  .servicios-header {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 56px;
    padding-bottom: 24px;
    border-bottom: 1px solid #E8ECF2;
  }

  .servicios-number {
    font-size: 0.7rem;
    font-weight: 700;
    color: #2F80FF;
    letter-spacing: 2px;
    background: #F0F5FF;
    padding: 4px 12px;
    border-radius: 4px;
  }

  .servicios-title {
    font-size: 1.8rem;
    font-weight: 700;
    color: #0A1628;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .servicios-subtitle {
    font-size: 0.9rem;
    color: #5E6472;
    margin: 0 0 0 auto;
  }

  .servicios-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px;
  }

  .serv-card {
    border: 1px solid #E8ECF2;
    border-radius: 16px;
    padding: 40px 36px;
    background: #FFFFFF;
    transition: all 350ms ease;
    display: flex;
    flex-direction: column;
  }

  .serv-card:hover {
    border-color: #C8D0DC;
    box-shadow: 0 8px 28px rgba(0,0,0,0.05);
    transform: translateY(-3px);
  }

  .serv-card-num-wrap { margin-bottom: 20px; }

  .serv-card-num {
    font-size: 0.75rem;
    font-weight: 700;
    color: #2F80FF;
    background: #F0F5FF;
    padding: 3px 10px;
    border-radius: 4px;
    letter-spacing: 1px;
  }

  .serv-card-title {
    font-size: 1.3rem;
    font-weight: 700;
    color: #0A1628;
    margin: 0 0 14px;
    line-height: 1.3;
  }

  .serv-card-summary {
    font-size: 0.92rem;
    line-height: 1.7;
    color: #5E6472;
    margin: 0 0 20px;
    flex: 1;
  }

  .serv-card-resolves {
    background: #F8F9FB;
    border-radius: 10px;
    padding: 16px 18px;
    margin-bottom: 20px;
    border-left: 3px solid #2F80FF;
  }

  .serv-card-resolves-label {
    display: block;
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: #2F80FF;
    margin-bottom: 6px;
  }

  .serv-card-resolves-text {
    font-size: 0.82rem;
    line-height: 1.6;
    color: #3A4050;
    margin: 0;
  }

  .serv-card-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .serv-card-item {
    font-size: 0.85rem;
    color: #3A4050;
    padding-left: 16px;
    position: relative;
    line-height: 1.5;
  }

  .serv-card-item::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: #2F80FF;
    opacity: 0.4;
  }

  @media (max-width: 640px) {
    .servicios-grid { grid-template-columns: 1fr; }
    .servicios-title { font-size: 1.4rem; }
    .servicios-subtitle { display: none; }
    .serv-card { padding: 28px 24px; }
  }
</style>
