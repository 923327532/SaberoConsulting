---
import type { SupportedLocale } from '../../i18n/utils/types';
import { siteSettings } from '../../data/settings/site';
import { getNavigation } from '../../data/navigation/index';
import { t } from '../../i18n/ui/index';
import { localizeNavHref } from '../../i18n/routes/map';

interface Props {
  currentLocale: SupportedLocale;
}

const { currentLocale } = Astro.props;
const navigation = getNavigation(currentLocale);
const currentYear = new Date().getFullYear();

interface SocialIcon {
  name: string;
  url: string;
  label: string;
  path: string;
  viewBox: string;
}

const socialIcons: SocialIcon[] = [
  {
    name: 'LinkedIn',
    url: siteSettings.social.linkedin || '#',
    label: 'LinkedIn',
    viewBox: '0 0 24 24',
    path: 'M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z',
  },
  {
    name: 'Instagram',
    url: siteSettings.social.instagram || '#',
    label: 'Instagram',
    viewBox: '0 0 24 24',
    path: 'M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z',
  },
  {
    name: 'YouTube',
    url: siteSettings.social.youtube || '#',
    label: 'YouTube',
    viewBox: '0 0 24 24',
    path: 'M23.498 6.186a3.016 3.016 0 00-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 00.502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 002.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 002.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z',
  },
  {
    name: 'Facebook',
    url: siteSettings.social.facebook || '#',
    label: 'Facebook',
    viewBox: '0 0 24 24',
    path: 'M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z',
  },
];

const legalLinks = [
  { key: 'footer.legal.privacy', href: '/privacidad' },
  { key: 'footer.legal.terms', href: '/terminos' },
  { key: 'footer.legal.cookies', href: '/cookies' },
  { key: 'footer.legal.cookiesPrefs', href: '#cookies-prefs' },
  { key: 'footer.legal.sitemap', href: '/sitemap-index.xml' },
];
---

<footer class="site-footer" role="contentinfo">
  <div class="footer-container">
    <!-- Main grid -->
    <div class="footer-grid">
      <!-- Brand block -->
      <div class="footer-brand">
        <a href={currentLocale === 'es' ? '/' : `/${currentLocale}/`} class="footer-logo-link" aria-label={siteSettings.name}>
          <img src={siteSettings.logo} alt={siteSettings.name} class="footer-logo-img" width="140" height="40" loading="lazy" />
        </a>
        <p class="footer-description">{t(currentLocale, 'footer.brand.description')}</p>
        <div class="footer-social">
          {socialIcons.map((social) => (
            <a
              href={social.url}
              class="footer-social-link"
              target="_blank"
              rel="noopener noreferrer"
              aria-label={social.label}
              title={social.label}
            >
              <svg width="18" height="18" viewBox={social.viewBox} fill="currentColor" aria-hidden="true">
                <path d={social.path} />
              </svg>
            </a>
          ))}
        </div>
      </div>

      <!-- Contact block -->
      <div class="footer-block">
        <h3 class="footer-block-title">{t(currentLocale, 'footer.contact.title')}</h3>
        <ul class="footer-block-list">
          <li>
            <a href={`mailto:${siteSettings.contact.email}`} class="footer-link footer-link-contact">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
              <span>{siteSettings.contact.email}</span>
            </a>
          </li>
          <li>
            <a href={`https://wa.me/${siteSettings.contact.whatsapp.replace('+', '')}`} class="footer-link footer-link-contact" target="_blank" rel="noopener noreferrer">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
              </svg>
              <span>{t(currentLocale, 'footer.contact.whatsapp')}</span>
            </a>
          </li>
          <li class="footer-contact-item">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
              <circle cx="12" cy="10" r="3"/>
            </svg>
            <span>{t(currentLocale, 'footer.contact.location')}</span>
          </li>
          <li>
            <a href={localizeNavHref('/contacto', currentLocale)} class="footer-link footer-link-cta">
              {t(currentLocale, 'footer.contact.form')}
            </a>
          </li>
        </ul>
      </div>

      <!-- Navigation block -->
      <div class="footer-block">
        <h3 class="footer-block-title">{t(currentLocale, 'footer.navigation.title')}</h3>
        <ul class="footer-block-list">
          {navigation.main.map((item) => (
            <li>
              <a href={localizeNavHref(item.href, currentLocale)} class="footer-link">
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Newsletter block -->
      <div class="footer-block footer-newsletter">
        <h3 class="footer-block-title">{t(currentLocale, 'footer.newsletter.title')}</h3>
        <p class="footer-newsletter-text">{t(currentLocale, 'footer.newsletter.text')}</p>
        <form class="footer-newsletter-form" action="#" method="post" data-newsletter>
          <div class="newsletter-input-group">
            <input
              type="email"
              name="email"
              class="newsletter-input"
              placeholder={t(currentLocale, 'footer.newsletter.placeholder')}
              aria-label={t(currentLocale, 'footer.newsletter.placeholder')}
              required
            />
            <button type="submit" class="newsletter-btn" aria-label={t(currentLocale, 'footer.newsletter.button')}>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <line x1="22" y1="2" x2="11" y2="13"/>
                <polygon points="22 2 15 22 11 13 2 9 22 2"/>
              </svg>
            </button>
          </div>
        </form>
        <p class="footer-newsletter-disclaimer">{t(currentLocale, 'footer.newsletter.disclaimer')}</p>
      </div>
    </div>

    <!-- Legal bottom bar -->
    <div class="footer-legal-bar">
      <div class="footer-legal-links">
        {legalLinks.map((link) => (
          <a
            href={link.href.startsWith('#') ? link.href : localizeNavHref(link.href, currentLocale)}
            class="footer-legal-link"
          >
            {t(currentLocale, link.key)}
          </a>
        ))}
      </div>
      <p class="footer-copyright">
        &copy; {currentYear} {siteSettings.name}. {t(currentLocale, 'footer.copyright')}
      </p>
    </div>
  </div>
</footer>

<script>
  // Newsletter form - prepared for future integration
  document.querySelectorAll('[data-newsletter]').forEach((form) => {
    form.addEventListener('submit', (e) => {
      e.preventDefault();
      // Placeholder: integrate with email service when ready
      const input = form.querySelector('.newsletter-input') as HTMLInputElement;
      if (input?.value) {
        // Future: send to API endpoint
        input.value = '';
        input.placeholder = 'Gracias por suscribirte!';
        setTimeout(() => {
          input.placeholder = 'Tu correo electronico';
        }, 3000);
      }
    });
  });
</script>

<style>
  /* ============================================
     SABERO Consulting - Premium Footer
     Inspired by Globant, NTT DATA, TCS
     ============================================ */

  .site-footer {
    background-color: var(--color-gray-900);
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    padding: var(--space-16) 0 0;
    position: relative;
  }

  .footer-container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  /* ---- Grid ---- */
  .footer-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr 1.2fr;
    gap: var(--space-10);
    padding-bottom: var(--space-12);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  /* ---- Brand ---- */
  .footer-logo-link {
    display: inline-flex;
    margin-bottom: var(--space-4);
  }

  .footer-logo-img {
    display: block;
    height: 36px;
    width: auto;
  }

  .footer-description {
    font-size: var(--text-sm);
    color: var(--color-gray-400);
    line-height: 1.7;
    margin-bottom: var(--space-5);
    max-width: 340px;
  }

  .footer-social {
    display: flex;
    gap: var(--space-2);
  }

  .footer-social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    color: var(--color-gray-400);
    background-color: rgba(255, 255, 255, 0.04);
    border-radius: var(--radius-md);
    transition: all var(--transition-fast);
    text-decoration: none;
  }

  .footer-social-link:hover {
    color: var(--accent-color);
    background-color: rgba(0, 102, 255, 0.1);
    transform: translateY(-1px);
  }

  .footer-social-link:focus-visible {
    outline: 2px solid var(--accent-color);
    outline-offset: 2px;
  }

  /* ---- Blocks ---- */
  .footer-block-title {
    font-size: var(--text-xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.2px;
    color: var(--accent-color);
    margin-bottom: var(--space-5);
    padding-bottom: var(--space-3);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .footer-block-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .footer-link {
    font-size: var(--text-sm);
    color: var(--color-gray-400);
    text-decoration: none;
    transition: color var(--transition-fast);
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-1) 0;
  }

  .footer-link:hover {
    color: var(--color-white);
  }

  .footer-link:focus-visible {
    outline: 2px solid var(--accent-color);
    outline-offset: 2px;
    border-radius: var(--radius-sm);
  }

  .footer-link-contact {
    gap: var(--space-2);
  }

  .footer-link-contact svg {
    flex-shrink: 0;
    opacity: 0.5;
  }

  .footer-link-contact:hover svg {
    opacity: 1;
    color: var(--accent-color);
  }

  .footer-contact-item {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    color: var(--color-gray-400);
    padding: var(--space-1) 0;
  }

  .footer-contact-item svg {
    flex-shrink: 0;
    opacity: 0.5;
  }

  .footer-link-cta {
    color: var(--accent-color);
    font-weight: 600;
    margin-top: var(--space-2);
  }

  .footer-link-cta:hover {
    color: var(--accent-hover);
  }

  /* ---- Newsletter ---- */
  .footer-newsletter-text {
    font-size: var(--text-sm);
    color: var(--color-gray-400);
    line-height: 1.6;
    margin-bottom: var(--space-4);
  }

  .newsletter-input-group {
    display: flex;
    gap: 0;
    border: 1px solid var(--color-gray-700);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: border-color var(--transition-fast);
  }

  .newsletter-input-group:focus-within {
    border-color: var(--accent-color);
    box-shadow: 0 0 0 3px rgba(0, 102, 255, 0.1);
  }

  .newsletter-input {
    flex: 1;
    padding: var(--space-3) var(--space-4);
    font-size: var(--text-sm);
    color: var(--color-white);
    background-color: transparent;
    border: none;
    outline: none;
    font-family: inherit;
  }

  .newsletter-input::placeholder {
    color: var(--color-gray-500);
  }

  .newsletter-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    flex-shrink: 0;
    background-color: var(--accent-color);
    color: var(--color-white);
    border: none;
    cursor: pointer;
    transition: background-color var(--transition-fast);
  }

  .newsletter-btn:hover {
    background-color: var(--accent-hover);
  }

  .newsletter-btn:focus-visible {
    outline: 2px solid var(--color-white);
    outline-offset: -2px;
  }

  .footer-newsletter-disclaimer {
    font-size: var(--text-xs);
    color: var(--color-gray-500);
    margin-top: var(--space-3);
    margin-bottom: 0;
  }

  /* ---- Legal bar ---- */
  .footer-legal-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-6) 0;
    gap: var(--space-6);
    flex-wrap: wrap;
  }

  .footer-legal-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-1);
  }

  .footer-legal-link {
    font-size: var(--text-xs);
    color: var(--color-gray-500);
    text-decoration: none;
    padding: var(--space-1) var(--space-2);
    border-radius: var(--radius-sm);
    transition: color var(--transition-fast);
    white-space: nowrap;
  }

  .footer-legal-link:hover {
    color: var(--color-gray-300);
  }

  .footer-legal-link:focus-visible {
    outline: 2px solid var(--accent-color);
    outline-offset: 2px;
  }

  .footer-copyright {
    font-size: var(--text-xs);
    color: var(--color-gray-500);
    margin-bottom: 0;
    white-space: nowrap;
  }

  /* ============================================
     Responsive
     ============================================ */

  /* Tablet landscape */
  @media (max-width: 1100px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-8);
    }

    .footer-brand {
      grid-column: 1 / -1;
    }

    .footer-description {
      max-width: 100%;
    }
  }

  /* Tablet portrait & mobile */
  @media (max-width: 768px) {
    .site-footer {
      padding-top: var(--space-12);
    }

    .footer-grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .footer-brand {
      grid-column: 1;
    }

    .footer-logo-img {
      height: 30px;
    }

    .footer-social {
      gap: var(--space-1);
    }

    .footer-social-link {
      width: 32px;
      height: 32px;
    }

    .footer-legal-bar {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-4);
      padding: var(--space-4) 0;
    }

    .footer-legal-links {
      gap: 0;
    }

    .footer-legal-link {
      padding: var(--space-1) var(--space-2);
    }

    .footer-copyright {
      white-space: normal;
    }
  }

  /* Small mobile */
  @media (max-width: 480px) {
    .footer-legal-links {
      flex-direction: column;
      gap: var(--space-1);
    }
  }
</style>
